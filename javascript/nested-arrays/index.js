const gameBoard = [
    ['X', '0', 'X'],
    ['O', null, 'X'],
    ['O', 'O', 'X']
]

console.log(gameBoard);

console.log(gameBoard[1][1]); //null
//para acessar os dados de uma nested array, é só colocar os colchetes. 